import 'package:flutter/material.dart';

/// Brand color constants.
///
/// This is the minimal seed for [AppTheme] — the full premium dark /
/// glassmorphism design system (gradients, blur surfaces, elevation
/// tokens, ...) is built in a later task.
class AppColors {
  const AppColors._();

  static const Color bottleGreen = Color(0xFF1A3D1F);
  static const Color brightGreen = Color(0xFF2D7A35);
  static const Color background = Color(0xFF0E1712);
}
